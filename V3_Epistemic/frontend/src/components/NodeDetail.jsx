import {format} from 'd3'; import {getColor} from '../utils/colors';
export function NodeDetail({node,onClose}){const money=format('$,.0f'); return <div style={{position:'fixed',top:70,right:10,zIndex:150,width:340,background:'rgba(5,10,20,0.85)',backdropFilter:'blur(12px)',padding:16,border:'1px solid rgba(99,102,241,0.3)',borderRadius:12}}><h3>{node.name}</h3><div><span style={{display:'inline-block',width:10,height:10,background:getColor(node.industry),borderRadius:'50%',marginRight:8}}/>{node.entity_type} · {node.industry}</div><hr/><div>Total Spend: {money(node.size||0)}</div><div>Total Filings: {node.filings||0}</div><div>Revolving Door: {node.revolving_door_count||0}</div><div>Gap Score: {node.gap_score||0}</div><div>Cluster: {node.cluster_label||'Unknown'}</div><button onClick={onClose}>× Close</button></div>}
