import {useState} from 'react'; import {INDUSTRY_COLORS} from '../utils/colors';
export function FilterPanel({data,filters,setFilters}){const [open,setOpen]=useState(false); const industries=Object.keys(INDUSTRY_COLORS); return <div style={{position:'fixed',top:70,left:10,zIndex:120}}><button onClick={()=>setOpen(!open)} style={{background:'rgba(5,10,20,0.85)',color:'#e2e8f0',border:'1px solid #334155',padding:'8px 12px'}}>≡ Filters</button>{open&&<div style={{marginTop:8,width:280,background:'rgba(5,10,20,0.85)',backdropFilter:'blur(12px)',padding:12,border:'1px solid #334155',borderRadius:12}}>{industries.map(i=><button key={i} onClick={()=>setFilters(f=>{const s=new Set(f.industries); s.has(i)?s.delete(i):s.add(i); return {...f,industries:s};})} style={{display:'block',width:'100%',textAlign:'left',marginBottom:4,background:INDUSTRY_COLORS[i],border:'none',padding:6,color:'#fff'}}>{i}</button>)}</div>}</div>}
